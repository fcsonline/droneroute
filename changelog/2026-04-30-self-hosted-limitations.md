## Summary

Hide features that don't make sense in the self-hosted version, which is designed as a single-account, personal instance.

## Changes

- Hide mission sharing UI (share button, unshare button, shared badge) in self-hosted mode
- Hide admin panel button in self-hosted mode
- Document self-hosted limitations in README
